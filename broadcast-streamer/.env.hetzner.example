# Copy to .env on the Hetzner box.
CONTROL_URL=https://100lights.com
# Must match the app's BROADCAST_AGENT_TOKEN (Vercel env).
AGENT_TOKEN=paste-a-long-shared-secret
WORKER_ID=hetzner-1
CAPACITY=6
# One YouTube stream key per channel this box may run. Non-secret RTMP URL + channel label live on the
# station (radio admin); only the KEY lives here.
KEYS={"cinematic":"key1","study-lofi":"key2","dnd-tavern":"key3"}
