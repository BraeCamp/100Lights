// Bump on EVERY change to public/apollo/engine.js. The version is appended to
// the worklet URL so browsers/CDNs can never pair a stale engine with new UI.
export const ENGINE_VERSION = '2026-08-20-14'
