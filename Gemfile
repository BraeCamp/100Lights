# Ruby tooling for the mobile release pipeline (Fastlane + CocoaPods). Pinned via Gemfile so
# local and CI use the same versions. Install with: bundle install
source 'https://rubygems.org'

gem 'fastlane'
gem 'cocoapods'
